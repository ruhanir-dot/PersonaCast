pydantic>=2.6         
openai>=1.30      
python-dotenv>=1.0    

tavily-python>=0.3    
arxiv>=2.1         

streamlit>=1.33
## new audix player, retirns the playback currentTime so we can anchor reaction
streamlit-advanced-audio>=0.1

# Audio generation (local only — uncomment to enable `--audio` and the app's audio button)
# Kept out of the hosted build so Streamlit Cloud stays script-only.
# kokoro-onnx>=0.4
# soundfile>=0.12

# faster tts not reliant on api, locally held much faster
## voice file not shipped download once for local usahe 
piper-tts >= 1.16 # TTS
faster-whisper >= 1.1 #STT

google-genai>=1.0

# agentic orchestration, no langchain bc we handle throttling by parsing gemini error messages!
langgraph >=0.6
websockets >= 12.0 
torch >= 2.2 
torchaudio>= 2.2
